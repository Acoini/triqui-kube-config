apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: traefik

resources:
  - namespace.yml

helmCharts:
  - name: traefik
    releaseName: traefik
    repo: https://traefik.github.io/charts
    version: 10.0.0
    valuesFile: values.yml
